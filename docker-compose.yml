version: '3.8'

services:
  web:
    build: .
    # container-name: ticket
    image: ticket:latest
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"